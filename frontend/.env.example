# Required: Backend API base URL (no trailing slash, no /api suffix — added by client)
NEXT_PUBLIC_API_URL=https://api.pakswap.com

# Set to "true" to bypass backend and use mock auth/data (for static preview only)
NEXT_PUBLIC_USE_MOCK=false
